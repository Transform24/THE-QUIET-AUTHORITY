name: YouTube Agent — Weekly Video

on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  generate-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic

      - name: Run YouTube Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python workflows/scripts/youtube_agent.py

      - name: Commit and push output to approval branch
        run: |
          git config user.name "TQA Agent"
          git config user.email "noreply@sanctuarygrace.com"
          git add workflows/output/
          if ! git diff --staged --quiet; then
            BRANCH="agent-output/youtube-$(date +%Y-%m-%d)"
            git checkout -b "$BRANCH"
            git commit -m "YouTube Agent: $(date +%Y-%m-%d) script draft — ready for review"
            git push -u origin "$BRANCH"
            echo "Draft pushed to $BRANCH — Grace reviews and merges"
          else
            echo "No new content to commit"
          fi
